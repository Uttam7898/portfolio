interface FastfolioPopupProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  hasReachedLimit?: boolean;
}

export function FastfolioPopup(_props: FastfolioPopupProps) {
  return null;
}
